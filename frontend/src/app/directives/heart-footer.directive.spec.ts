import { HeartFooterDirective } from './heart-footer.directive';

describe('HeartFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new HeartFooterDirective();
    expect(directive).toBeTruthy();
  });
});
